// Declare your function here
function createLongGreeting(nom, age) {
  return "hello my name is " + nom + "my age is " + age;
}

const greeting = createLongGreeting("Daniel", 31);

console.log(greeting);
